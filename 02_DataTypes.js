"use strict"; // treat all JS codea as newer version
// once "use strict" is written there is no going back by typing "unuse strict"

// alert (3 + 3)// we are using nodejs, not browser

console.log(3 + 3); // code should be neat and readable

let name = "rajesh";
let age = 27;
let isLoggedIn = false;

// number => 2 to power 53
// bigInt
// string => ""
// boolean => true/false
// null => standalone value   (important)
// undefiend => value not assigned
// symbol => unique

// object

console.log(typeof null);
console.log(typeof undefined);
